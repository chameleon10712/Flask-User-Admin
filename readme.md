# Flask Login + User Admin Example
This code sample makes use of Flask and Flask-SQLAlchemy, a library that manages User registration, log in, session, logout, user administration functionality.

## Requirements.txt
    Flask
    Flask-SQLAlchemy


